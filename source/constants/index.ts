/** Check if the NODE_ENV equals to "development". */
export const __DEV__ = process.env.NODE_ENV === 'development';

/** Check if the NODE_ENV equals to "production". */
export const __PROD__ = process.env.NODE_ENV === 'production';
